# micro controller: esp32-e  v1.0

# Installation process:
pip install esptool
python -m esptool version
# erase existing firmware on micro controller:
# python -m esptool --chip esp32 --port COM3 erase_flash
# python -m esptool --chip esp32 --port COM3 --baud 460800 write_flash -z 0x1000 ESP32_GENERIC-20260406-v1.28.0.bin


pip install mpremote
mpremote connect COM3 fs cp main.py :
python -m mpremote connect COM3 fs cp main.py :
python -m mpremote connect COM3 fs cp main.py : fs cp index.html :



in index.html directory:
python -m http.server 8000
in browser:
http://localhost:8000/


wifi setup: -> esp32 acts as wifi access point
ssid: ESP32-DASHBOARD
pw: 12345678
in browser: http://192.168.4.1

works but only updates when i press refresh. the bluetooth setup did not show updated numbers because desktop pc did not have bluetooth. but could try again with laptop that has bluetooth as well

# TODO:
- i always need to press the reset button. is it possible without?
- fix password access
- if weight == 0: the weight should stay constant to the last know value
        because weight == 0 could mean that currently cup is used for drinking
- if tilt is bigger than a certain value, make a red blinking frame around the graph
- add statistics: how much was drunk in the last 10 seconds. (absolute values). maybe also save the time when it was drunk
        in a table
- add a high-score-streak:
- add reminder
- add daily goal



präsi:
nicht zu lange mit einführung aufhalten
juroren wissen alles schon von der "challenge" webseite
gerne auch die mitgebrachten, vorgegebenen becher transparent vorzeigen



{
    "address": "COM3",
    "open_on_start": true
}


ESP32 (MicroPython BLE)
        ↓
Bluetooth LE notifications
        ↓
Chrome Web Bluetooth API
        ↓
index.html dashboard UI



eichung der wiege:
170 g in echt entspricht 125 g auf dem computer




FIX:
5. Your socket server is blocking

Currently:

conn, addr = s.accept()

blocks forever until a browser connects.

This is okay for your hackathon/demo setup.

You only need async/non-blocking later if:

sensors update independently
multiple users connect
real-time streaming is needed

For now:

your current architecture is fine
simple = good


->
add s.settimeout(0.1) after s.listen(5):
s.listen(5)
s.settimeout(0.1)

replace:
conn, addr = s.accept()

with

try:
    conn, addr = s.accept()
except OSError:
    conn = None



https://chatgpt.com/share/6a10c208-c4a0-83eb-937e-b0287c01ce4e

Important note
GPIO12 can sometimes cause ESP32 boot problems because it is a strapping pin.
If ESP32 fails to boot later, move SCK from GPIO12 to something safer like: